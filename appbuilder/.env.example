# Copy this file to `.env` and fill in real values. Do NOT commit `.env`.
# The easiest way to populate the Adobe I/O Runtime values is: `aio app use`
# (select your Org > Project > Workspace in the Adobe Developer Console).

## Adobe I/O Runtime credentials (required for `aio app run` / `deploy`)
AIO_runtime_auth=
AIO_runtime_namespace=
AIO_runtime_apihost=https://adobeioruntime.net

## Action secrets (required by specific actions in app.config.yaml)
# Shared secret the JSON2HTML worker sends as the `x-api-key` header to the team-proxy action.
SERVICE_API_KEY=
CRM_API_TOKEN=
# Microsoft Teams incoming webhook (Power Automate Workflows: "Send webhook alerts to a channel").
TEAMS_WEBHOOK_URL=
# SITE_LIVE_HOST is NOT set here: it is a request parameter. The GitHub workflow derives and
# sends it (main--<repo>--<owner>.aem.live); for manual curl tests, include it in the JSON body.
# Only notify when the published page's cq-tags meta contains this tag (leave blank to notify all).
REQUIRED_TAG=universal-editor-site:priority/high
